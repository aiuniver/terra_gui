import cv2


def video(**params):
    flag = True

    def fun():
        if flag:
            # writer = cv2.VideoWriter(
            #     path, cv2.VideoWriter_fourcc('M', 'J', 'P', 'G'), fps, frameSize[, isColor]
            # )
            flag != flag

    return fun
